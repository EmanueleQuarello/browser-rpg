# Leave empty in local dev: Vite proxies /api to http://127.0.0.1:3001
# Production (Vercel): origin of the Render API, no trailing slash.
VITE_API_URL=
# Optional Sentry browser DSN (set on Vercel; leave empty locally)
# VITE_SENTRY_DSN=
